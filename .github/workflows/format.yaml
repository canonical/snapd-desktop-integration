name: Formatting

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Verify formatting
        run: |
          export SOURCE_PATHS="src tests"
          ./format-source.sh check
